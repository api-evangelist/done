overlay: 1.0.0
info:
  title: API Evangelist enhancements — _done DNS & WHOIS Lookup
  version: 1.0.0
extends: openapi/done-dns-whois-lookup-openapi.yml
x-generated: '2026-08-11'
x-method: generated
x-source: openapi/_original/done-dns-whois-lookup-openapi.json
x-note: 'Non-destructive. Captures API Evangelist enhancements only: catalog provenance, a canonical resource tag
  in place of the SEO keyword tag list (keywords preserved under x-keywords), and a service-namespaced canonical
  operationId. The provider spec is never mutated.'
actions:
- target: $.info
  description: Record API Evangelist provenance and the catalog identity of this service.
  update:
    x-api-evangelist:
      provider: _done
      slug: done
      catalog: https://apis.io/provider/done/
      harvested: '2026-08-11'
      source: https://dns-whois-lookup.underscoredone.com/openapi.json
      payment_protocol: x402
      price_usd: 0.01
- target: $.paths["/lookup"].post
  description: Collapse 15 SEO keyword tags to one canonical resource tag "dns lookup". The original keyword list
    is preserved as x-keywords so nothing is lost.
  update:
    tags:
    - dns lookup
    x-keywords:
    - dns lookup
    - whois lookup
    - dns
    - whois
    - domain lookup
    - dns records
    - nameservers
    - mx records
    - registrar lookup
    - rdap
    - domain registration
    - txt records
    - a record lookup
    - domain intelligence
    - bulk dns
- target: $.paths["/lookup"].post
  description: Namespace the operationId by service — operationIds collide across the 26 _done specs.
  update:
    x-canonical-operation-id: dns_whois_lookup_handler_lookup_post
- target: $.tags
  description: Reduce the 15-entry SEO tag list to one canonical resource tag.
  update:
  - name: dns lookup
    description: DNS & WHOIS Lookup
