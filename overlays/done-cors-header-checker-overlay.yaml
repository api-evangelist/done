overlay: 1.0.0
info:
  title: API Evangelist enhancements — _done CORS Header Checker
  version: 1.0.0
extends: openapi/done-cors-header-checker-openapi.yml
x-generated: '2026-08-11'
x-method: generated
x-source: openapi/_original/done-cors-header-checker-openapi.json
x-note: 'Non-destructive. Captures API Evangelist enhancements only: catalog provenance, a canonical resource tag
  in place of the SEO keyword tag list (keywords preserved under x-keywords), and a service-namespaced canonical
  operationId. The provider spec is never mutated.'
actions:
- target: $.info
  description: Record API Evangelist provenance and the catalog identity of this service.
  update:
    x-api-evangelist:
      provider: _done
      slug: done
      catalog: https://apis.io/provider/done/
      harvested: '2026-08-11'
      source: https://cors-header-checker.underscoredone.com/openapi.json
      payment_protocol: x402
      price_usd: 0.01
- target: $.paths["/check"].post
  description: Collapse 15 SEO keyword tags to one canonical resource tag "cors headers". The original keyword list
    is preserved as x-keywords so nothing is lost.
  update:
    tags:
    - cors headers
    x-keywords:
    - cors headers
    - cors
    - cross origin
    - access-control-allow-origin
    - cors policy
    - cross origin resource sharing
    - is cors enabled
    - cors checker
    - cors misconfiguration
    - preflight check
    - browser cors error
    - allow-origin header
    - credentialed cors
    - cors audit
    - cors test
- target: $.paths["/check"].post
  description: Namespace the operationId by service — operationIds collide across the 26 _done specs.
  update:
    x-canonical-operation-id: cors_header_checker_handler_check_post
- target: $.tags
  description: Reduce the 15-entry SEO tag list to one canonical resource tag.
  update:
  - name: cors headers
    description: CORS Header Checker
